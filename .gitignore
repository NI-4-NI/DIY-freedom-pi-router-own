# macOS
.DS_Store
._*

# editor
*.swp
*.swo
.vscode/
.idea/

# local state (never commit install state that may contain secrets)
install.conf
*.key
*.pem

# build/test artifacts
*.log
*.tmp
# Ruflo (local agent tooling, not part of the installer)
.claude/
.claude-flow/
CLAUDE.md
agentdb.rvf
agentdb.rvf.lock
.ruflo/
