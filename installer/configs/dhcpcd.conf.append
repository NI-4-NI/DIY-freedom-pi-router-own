
# === freedom-pi router config ===
# eth0 (Pi built-in) = WAN, gets DHCP from modem automatically, no config needed here.

interface eth1
static ip_address={{LAN_GATEWAY}}/24

interface wlan0
static ip_address={{WIFI_GATEWAY}}/24
nohook wpa_supplicant
